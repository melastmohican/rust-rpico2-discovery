# Project Rules

- Always run the full CI suite (`cargo fmt --all -- --check`, `cargo clippy --examples -- -D warnings`, `cargo check --examples`) and verify zero errors before pushing any commits to GitHub.
